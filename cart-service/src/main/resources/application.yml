spring:
  application:
    name: cart-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-curly-base-a8rzjasw-pooler.eastus2.azure.neon.tech:5432/neondb?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:neondb_owner}
    password: ${SPRING_DATASOURCE_PASSWORD:npg_RIPTxsdr6a8j}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8084

# JWT Configuration (matching existing pattern)
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# Service URLs for inter-service communication
services:
  product: http://product-service:8083
  auth: http://auth-service:8081
  order: http://order-service:8082

management:
  endpoints:
    web:
      exposure:
        include: health,info
